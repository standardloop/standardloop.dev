---
version: '3'

tasks:
  default:
    cmds:
      - echo "Hello World"
    silent: true

  build:
    deps:
      - clean
    cmds:
      - zensical build

  run:
    deps:
      - clean
    aliases: [serve, server]
    cmds:
      - zensical serve

  clean:
    cmds:
      - rm -r ./.cache 
